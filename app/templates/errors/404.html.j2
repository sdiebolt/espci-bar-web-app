{% extends "base.html.j2" %}

{% block app_content %}
  <div class="container">
    <div class="row">
      <div class="col-lg-6 col-md-6 col-sm-8 col-xs-8 col-lg-offset-4 col-md-offset-3 col-sm-offset-3 col-xs-offset-2">
        <h1>Oh snap!</h1>
        <h2>Something went wrong</h2>
        Sorry, the page you requested wasn't found.<br><br>
        <div class="text-left">
        <a href="{{ url_for('main.index') }}" class="btn btn-primary btn-lg">
          <span class="glyphicon glyphicon-home"></span> Take Me Home
        </a>
      </div>
      </div>
    </div>
  </div>
{% endblock %}
