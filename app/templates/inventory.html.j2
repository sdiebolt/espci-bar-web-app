{% extends 'base.html.j2' %}

{% block app_content %}
<!-- Items sorting -->
<div class="container my-1">
  <div class="btn-toolbar justify-content-between" role="toolbar" aria-label="Item sorting">
    <div class="btn-group" role="group" aria-label="Alphabet sorting">
      <a class="btn btn-outline-primary{% if sort == 'asc' %} active{% endif %}" href="{{ url_for('main.inventory', sort='asc') }}" data-original-title="Sort By Alphabet" role="button"><i data-feather="arrow-down"></i></a>
      <a class="btn btn-outline-primary{% if sort == 'desc' %} active{% endif %}" href="{{ url_for('main.inventory', sort='desc') }}" data-original-title="Sort By Alphabet" role="button"><i data-feather="arrow-up"></i></a>
    </div>
    <div class="btn-group" role="group" aria-label="Add item">
      <a class="btn btn-primary" href="{{ url_for('main.add_item') }}" data-original-title="Sort By Alphabet" role="button"><i data-feather="plus"></i></a>
    </div>
  </div>
</div>

<div style="margin-top:10px"></div>

<div class="table-responsive">
  <table class="table table-striped table-bordered">
    <thead>
      <tr>
        <th>Name</th>
        <th>Quantity</th>
        <th>Price</th>
        <th>Alcohol</th>
        <th>Favorite</th>
        <th>Edit</th>
      </tr>
    </thead>
    <tbody>
      {% for item in inventory.items %}
      <tr {% if item.quantity <= 0 and item.is_quantifiable %}class="table-danger"{% elif item.id == quick_access_item.id %}class="table-success"{% endif %}>
        <td class="align-middle">{{ item.name }}</td>
        <td class="align-middle">
          {% if item.is_quantifiable %}
          {{ item.quantity }}
          {% else %}
          Unquantifiable
          {% endif %}
        </td>
        <td class="align-middle">{{ item.price }}€</td>
        <td class="align-middle">
          {% if item.is_alcohol %}
          <i data-feather="check"></i>
          {% else %}
          <i data-feather="x"></i>
          {% endif %}
        </td>
        <td class="align-middle">
          {% if item.is_favorite %}
          <i data-feather="star"></i>
          {% endif %}
        </td>
        <td class="align-middle">
          <div class="btn-group" role="group" aria-label="Item actions">
            <a class="btn btn-success{% if item.id == quick_access_item.id %} disabled{% endif %}" href="{{ url_for('main.set_quick_access_item', item_name=item.name) }}" data-original-title="Set quick access item" role="button"><i data-feather="zap"></i></a>
            <a class="btn btn-warning" href="{{ url_for('main.edit_item', item_name=item.name) }}" data-original-title="Edit item" role="button"><i data-feather="edit"></i></a>
            <button class="btn btn-danger dropdown-toggle" type="button" id="dropdownDeleteItem" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i data-feather="x-square"></i>
            </button>
            <div class="dropdown-menu" aria-labelledby="dropdownDeleteItem">
              <h6 class="dropdown-header">Delete this item?</h6>
              <a class="dropdown-item" href="{{ url_for('main.delete_item', item_name=item.name) }}">Yes</a>
              <a class="dropdown-item" href="#">No</a>
            </div>
          </div>
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</div>

<nav aria-label="Page navigation">
  <ul class="pagination justify-content-center">
    {%- for page in inventory.iter_pages() %}
    <li class="page-item{% if page == inventory.page %} active{% endif %}{%if not page %} disabled{% endif %}">
      <a class="page-link" href="{{ url_for('main.inventory', page=page, sort=sort) }}">
        {% if page %}
        {{ page }}
        {% else %}
        <span class=ellipsis>…</span>
        {% endif %}
      </a>
      {%- endfor %}
    </li>
  </ul>
</nav>
{% endblock %}
