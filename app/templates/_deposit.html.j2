<!-- User deposit modal button -->
<button type="button" class="btn btn-secondary" data-toggle="modal" data-target="#deposit-modal" data-name="{{ user.first_name }} {{ user.last_name }}" data-url="{{ url_for('main.deposit', username=user.username) }}">
  Deposit
</button>
